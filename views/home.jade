extends layout
block content
    .container
        .row
            .col-md-3
                form(role='form', action='/add-users', method='post')
                    .form-group
                        label(for='role') User
                        input#text.form-control(type='text', name='username')
                        label(for='role') Role
                        input#text.form-control(type='text', name='role')
                    button.btn.btn-default(type='submit') Add User Role

